{
  "name": "backend-homework",
  "version": "1.0.0",
  "description": "Backend homework with MongoDB connection and Docker Compose setup",
  "main": "connect-mongodb.js",
  "scripts": {
    "dev": "node --watch-path app server.js",
    "start": "node server.js",
    "test": "node --test test/**/*.test.js",
    "test:watch": "node --test --watch test/**/*.test.js"
  },
  "engines": {
    "node": ">=25.0.0"
  },
  "keywords": [
    "mongodb",
    "nodejs",
    "backend",
    "database",
    "docker"
  ],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^5.1.0",
    "mongodb": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
